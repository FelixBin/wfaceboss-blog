server:
  port: 8080
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/blog_dev?useUnicode=true&characterEncoding=utf-8
    username: root
    password: 123456
  # 数据库操作
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  #日志
  logging:
    level:
      root: info
      top.wfaceboss: debug
    file: log/blog-dev.log